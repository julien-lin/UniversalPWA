# @universal-pwa/cli

CLI commander : commandes `scan`, `generate`, `inject`, `preview`. Sert aussi d’API locale (H3/Express léger) pour la web UI. S’appuie sur `@universal-pwa/core` et les templates.

