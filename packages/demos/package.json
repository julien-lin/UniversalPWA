{
  "name": "universal-pwa-demos",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "echo \"E2E désactivés temporairement\"",
    "test:lighthouse": "lighthouse http://localhost:3000 --only-categories=pwa --output=json --output-path=./lighthouse-report.json",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "lighthouse": "^13.0.1",
    "playwright": "^1.57.0",
    "serve": "^14.2.5",
    "vitest": "^2.1.0"
  }
}
