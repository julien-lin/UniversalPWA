# @julien-lin/universal-pwa-web-ui

Web UI no-code (Vite + React 19 + Tailwind) pour piloter le CLI : scan, config manifest/SW, injections HTML, preview. Communique via l'API locale exposée par `@julien-lin/universal-pwa-cli`.

