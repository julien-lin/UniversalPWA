import { describe, it, expect } from 'vitest'
import { placeholder } from './index.js'

describe('@julien-lin/universal-pwa-templates', () => {
  it('should export placeholder', () => {
    expect(placeholder).toBe(true)
  })
})

