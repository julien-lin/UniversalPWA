name: CI - Code Quality Validation

# ✅ Vérifie les règles de qualité code sur push et PR
# 📅 Date: 29 janvier 2026 - Publication manuelle NPM
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  validate:
    name: Validate (Lint + Typecheck + Test)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Clear TypeScript cache
        run: |
          rm -rf packages/*/tsconfig.tsbuildinfo
          rm -rf packages/*/.tsbuildinfo
          find packages -name "*.tsbuildinfo" -delete || true

      - name: Build (for type resolution in lint: templates → core)
        run: pnpm build

      - name: Lint
        run: pnpm -r lint

      - name: Type check
        run: pnpm -r typecheck

      - name: Test
        run: pnpm -r --filter '!universal-pwa-demos' test
